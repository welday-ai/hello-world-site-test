# Welday Enterprises Links

Bookmark dashboard for Welday ventures and apps.

- `index.html`: searchable links page
- `data/links.json`: venture and app URLs extracted from dashboard data

Run locally:

```bash
python -m http.server 8000
```

Open: `http://localhost:8000/`
